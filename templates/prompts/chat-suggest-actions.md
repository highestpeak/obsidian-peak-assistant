Based on this conversation, suggest 2-3 actionable next steps the user might take. These should be concrete actions (not questions), such as "Search vault for related notes", "Summarize key points", "Create action items from discussion", "Find contradicting viewpoints".

Keep suggestions short (under 8 words each), specific to the conversation content, and in the same language as the conversation.

Conversation (last few messages):
{{messages}}

Return a JSON array of objects:
```json
[{"label": "Short action label", "prompt": "The full prompt to submit to the assistant"}]
```

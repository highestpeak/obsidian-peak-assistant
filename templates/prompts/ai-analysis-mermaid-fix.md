The following Mermaid code failed validation.

**Parser error:** {{validationError}}

**Invalid code:**
```
{{invalidCode}}
```

Output only the corrected Mermaid diagram in a single \`\`\`mermaid ... \`\`\` block. No other text.

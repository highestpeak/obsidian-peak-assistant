{{candidateJson}}

Should this candidate be materialized as a hub_doc (navigation summary)? Consider graph signals (graphScore, role, sourceKind) only.

Return JSON: { "accept": boolean, "confidence": number, "reason": string }

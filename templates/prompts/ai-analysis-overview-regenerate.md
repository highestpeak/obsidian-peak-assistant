# Context
- **Query**: {{originalQuery}}
- **Current analysis snapshot**:
<<<
{{{currentResultSnapshot}}}
>>>

# Task
Produce one Mermaid overview diagram that reflects the above: key facts, conflicts or tensions, and conclusions. Use flowchart only for causal/dependency; use mindmap for hierarchy, quadrant for comparison, timeline for stages. Partition with subgraphs (e.g. Known facts / Conflicts / Conclusions). ≤15 nodes; label length ≤30 chars or use `<br>`; use `<br>` for line breaks in labels (not `\n`). Output valid Mermaid code only, no commentary.

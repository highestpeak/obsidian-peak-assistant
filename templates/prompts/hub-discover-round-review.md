Round summary (JSON):

{{roundSummaryJson}}

Return JSON with keys: coverageSufficient (boolean), quality ("good"|"acceptable"|"poor"), needAnotherRound (boolean), confidence (0-1), summary (string), strengths (string array), issues (string array), nextDirections (string array), suggestedDiscoveryModes (array of folder|document|cluster|manual_seed), targetPathPrefixes (string array), stopReason (string).

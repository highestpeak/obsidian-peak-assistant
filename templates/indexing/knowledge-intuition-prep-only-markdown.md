# {{vaultName}} Intuition Skeleton v{{dateLabel}} (prep only)

_Structured LLM pass was skipped (`stopAt: prep`). Run without `stopAt` to synthesize all sections._

## Deterministic backbone excerpt

{{{backboneExcerpt}}}

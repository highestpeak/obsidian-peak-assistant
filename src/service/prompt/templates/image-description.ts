/**
 * Image description prompt.
 * Describes images using vision models.
 */
export const template = `Please describe this image in detail, including any text visible in the image (OCR), objects, scenes, and any other relevant information.`;

export const expectsJson = false;